// Kelime sayısı uygulaması.

/*let metin = 'Suanda Yalovada java egıtımındeyım.';

let harf = prompt('Harf giriniz.');

let sonuc = bul(harf);
alert('Harf sayısı :' + sonuc);
bul('a');


function bul(harf){
    toplam=0;
    for(let i = 0 ; i<metin.length; i++){
        if(metin.charAt(i).toLowerCase()===harf.toLowerCase()){
            toplam+=1;
        }
    }
    return toplam;
    //console.log('Harf Sayısı : ' + toplam);
}
*/
// toLowerCase  buyuk kucuk harfi algılamak ıcın kullanılır.